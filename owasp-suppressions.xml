<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">
    <!--
    OWASP Dependency Check Suppressions
    This file suppresses known false positives or accepted vulnerabilities
    -->

    <!-- Suppress H2 database vulnerabilities (only used for testing) -->
    <suppress>
        <notes><![CDATA[
        H2 database is only used for testing purposes and not in production environment
        ]]></notes>
        <packageUrl regex="true">^pkg:maven/com\.h2database/h2@.*$</packageUrl>
    </suppress>

    <!-- Example: Suppress specific CVE if it doesn't apply to your usage -->
    <!--
    <suppress>
        <notes><![CDATA[
        This CVE is related to a specific usage pattern that doesn't apply to our application
        ]]></notes>
        <filePath regex="true">.*commons-lang3.*\.jar</filePath>
        <cve>CVE-2025-48924</cve>
    </suppress>
    -->

</suppressions>
